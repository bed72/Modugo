name: modugo
version: 4.2.8
homepage: https://github.com/bed72/Modugo
repository: https://github.com/bed72/Modugo
description: Modular Routing and Dependency Injection for Flutter with GoRouter.

screenshots:
  - description: "Modugo Logo"
    path: images/logo.png
topics:
  - modugo
  - routing
  - injection
  - navigation
  - deep-linking

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.1"

dependencies:
  flutter:
    sdk: flutter

  go_router: ^17.1.0
  path_to_regexp: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  mocktail: ^1.0.4
  flutter_lints: 6.0.0
